const { expect } = require('chai')
const sinon = require('sinon')
const rbacModel = require('../../../../server/model/admin/rbac.js')


describe('rbac', () => {
  describe('test createOrUpdateUserRole function', ()=>{
    it('should be tree if length is 2', async () => {
      /*
      let result = await rbacModel.createOrUpdateUserRole('10411304', ['ce:ee', 'rd:me']);
      expect(result.length).equals(2);
      */
    })
  })
});
