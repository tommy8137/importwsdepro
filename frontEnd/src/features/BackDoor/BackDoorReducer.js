import { handleActions } from 'redux-actions';
import { actionTypes } from './BackDoorActions';


const initialState = {
//   isAllowed: false
};

export default handleActions({
//   [actionTypes.BACK_DOOR___UPDATE_PERMISSION]: (state, payload) => {
//     const { status } = payload;
//     return {
//       ...state,
//       isAllowed: status
//     };
//   },

}, initialState);

